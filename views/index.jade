extends layout

block content
  h1= title
  
  form(method="POST", action="/", enctype="multipart/form-data")
    input(type="file", name="file")
    button.submit(type="submit") Upload

  br
  video(src= url, type= type)
